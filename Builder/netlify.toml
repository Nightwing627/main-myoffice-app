[build]
  #command = "npm run build:cloud" # the command you run to build this file
  functions = "dist/server" # netlify-lambda builds to this folder AND Netlify reads functions from here
  publish = "build/" # create-react-app builds to this folder, Netlify should serve all these files statically